<%= form_for @message do |f| %>

<%= f.label "To #{@thing.user.name}" %>
<%= f.hidden_field :to_id, :value => "#{@thing.user_id}" %>
<br>

<%# f.label :to_id %>
<%# f.text_field :to_id, :placeholder => 'luke', :value => @thing.user_id %>
<br>

<%= f.label :subject %>
<%= f.text_field :subject %>
<br>

<%= f.label :message %>
<%= f.text_area :message %>
<br>

<%= f.submit %>
<% end %>
